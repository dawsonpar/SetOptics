# Copy to .env at the repo root, then fill in your key(s).
# Required for any LLM annotation or LLM-enabled ensemble mode.
GEMINI_API_KEY=

# Optional alias. Some scripts read GOOGLE_API_KEY first.
GOOGLE_API_KEY=
